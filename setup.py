from setuptools import setup

version = '0.1.3'

setup(
    name='runtime',
    version=version,
    description='app runtime management',
    author='Andrey Popp',
    author_email='8mayday@gmail.com',
    py_modules=['runtime'],
    test_suite='tests',
    zip_safe=False)
